# Douyin Download API
douyin:
    api_base: '${DOUYIN_API_BASE:-http://localhost:8081}'
    cookie_file: 'config/douyin_cookie.txt'
    download_timeout: 120

# ffmpeg Processing
ffmpeg:
    default_crf: 23
    preset: 'medium'             # ultrafast, fast, medium, slow
    audio_bitrate: '128k'

# LLM Translation
# API key is NOT in this file — keys are entered in Settings → API Keys in
# the web UI, persisted to browser localStorage, and sent with each request.
translation:
    backend: 'anthropic'                    # "anthropic" | "openai" | "deepseek"
    model: 'claude-sonnet-4-20250514'       # model ID
    full_document_threshold: 100         # single LLM call up to this many segments
    chunk_size: 50                       # segments per chunk when exceeding threshold
    temperature: 0.7
    # When true (default), the translator is instructed to mark OCR noise
    # (watermarks, channel handles, stray fragments) with __SKIP__. Those
    # segments are then dropped from the final SRT entirely. Set false if
    # you find it removing legit content.
    skip_noise: true
    default_profile: 'funny-casual-vi'

# OCR Subtitle Extraction (RapidOCR / ONNX Runtime)
ocr:
    fps: 2.0                          # Frames per second to extract
    crop_bottom_pct: 0.25             # Pre-crop to bottom 25% of frame (0 = disabled)
    confidence_threshold: 0.7         # Minimum OCR confidence
    similarity_threshold: 0.85        # SequenceMatcher ratio for dedup
    # ONNX Runtime execution provider: auto | cpu | cuda | directml | coreml.
    # 'auto' picks the best available (CUDA > DirectML > CoreML > CPU).
    # NVIDIA GPU requires installing the [gpu] extra and running docker
    # with `--gpus all`.
    execution_provider: auto
    subtitle_region:
        min_y: 0.65                   # Only text below 65% of frame height
        max_watermark_frequency: 0.80 # Text in >80% of frames = watermark
        min_text_height: 0.02        # Min text height as fraction of frame
        horizontal_margin: 0.10       # Ignore text in outer 10% left/right

# TTS Dubbing
tts:
    enabled: true
    default_provider: google            # "google", "gemini", "openai", "elevenlabs"
    # Underlay level (dB) for the original-language audio mixed under the dub.
    # 0 disables the underlay entirely. Default: -12 (audible undertone).
    # Request bodies / UI settings override this value.
    underlay_db: ${TTS_UNDERLAY_DB:--18.0}
    # Gemini Audio Generation API (separate from Google Cloud TTS).
    # Uses a Google AI Studio API key — enable the Generative Language
    # API in your AI Studio project, NOT the same key as Google Cloud TTS.
    # API keys are typically supplied per-request via the UI/Settings instead.
    gemini_model: 'gemini-2.5-flash-preview-tts'

# Pipeline
pipeline:
    data_dir: './data'
    max_concurrent: 3
    retry_max_attempts: 3
    retry_base_delay: 5
    skip_existing: true
